# 2026-06-02 10:31 — ESM Phase E2 DOM facade

Repo: `/home/longnick/projects/xekho`
Branch: `test/xe-kho-repo-implementer-skill`
Task: Continue safe refactor progress and report status.

## Summary

Started ESM Phase E2 with the safest leaf utility facade:

- Added `app/esm/utils/dom.js` as an importable ESM facade for `escapeHtml()`.
- Added `installGlobalDomUtils()` so the ESM facade can preserve `window.XekhoApp.utils.dom.escapeHtml()` compatibility.
- Updated `app/esm/main.js` to import the DOM facade and mark `window.XekhoApp.esm.facades.dom` readiness.
- Updated `index.html` module script cache key from `20260602-e1` to `20260602-e2-dom`.
- Updated `scripts/verify-esm-entry.js` for the imported facade.
- Added `scripts/verify-esm-dom-utils.js` with a native dynamic-import smoke test.

## Safety

- Backup created: `/home/longnick/backups/xekho-esm-e2-dom-20260602-102850`.
- Did not import or convert `app.js`.
- Did not change POS order/data/payment/customer flows.
- Did not touch secrets, `.env`, production database, migration files, or raw media.
- Root `package.json` remains `commonjs`.

## Verification

Targeted verification already passed during implementation:

```bash
node scripts/verify-esm-dom-utils.js
node scripts/verify-esm-entry.js
```

Full verification should include syntax checks, tsc, lint, tests, Vite build, and final git status before commit/report.

## Progress update

- Overall long-term refactor progress: ~68%.
- Core non-optional refactor/security/testing progress: ~74%.
- Near-term safe execution: ~99.5%.
- ESM readiness: ~40%.

## Next candidate

ESM Phase E2 second leaf facade: `app/utils/format.js` → `app/esm/utils/format.js`, while preserving legacy formatter globals used by `store.js`.
